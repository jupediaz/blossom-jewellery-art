# Launch Ready Report — Blossom by Olha — 2026-03-11

## Overall Score: 74/100

| Domain | Score | Critical | Warnings | Fixed | Status |
|----------------|-------|----------|----------|-------|--------|
| Security | 82 | 2 | 3 | 7 | Fixed: CSP+HSTS, webhook guards, mass-assignment, open endpoints, HTML injection, email error handling |
| Performance | 88 | 0 | 2 | 0 | OK for launch |
| SEO | 92 | 0 | 1 | 0 | Excellent |
| Accessibility | 72 | 1 | 3 | 2 | Fixed: skip-to-content, error boundary. Remaining: html lang SSR, aria coverage |
| Dependencies | 72 | 1 | 2 | 0 | npm audit fix running |
| Email | 72 | 4 | 3 | 3 | Fixed: webhook try/catch, canonical URLs, cron URL. Remaining: unsubscribe, welcome trigger |
| Code Quality | 75 | 3 | 5 | 5 | Fixed: idempotency, webhook secret, rollback qty, register validation, console.log |
| i18n | 82 | 0 | 2 | 0 | Remaining: footer hardcoded strings, privacy/terms pages |
| Orthography | 95 | 1 | 0 | 4 | Fixed: all 4 Spanish errors |
| Database | 72 | 2 | 1 | 0 | Remaining: indexes, transaction wrapping |
| Stripe | 82 | 2 | 2 | 1 | Fixed: idempotency guard. Remaining: checkout idempotency key, transaction wrapping |
| Credentials | 95 | 0 | 1 | 0 | Excellent |
| Links | 85 | 1 | 1 | 1 | Fixed: canonical email URLs. Remaining: og-image.jpg |
| Role Empathy | 75 | 3 | 0 | 1 | Fixed: error boundary. Remaining: password reset, mobile admin sidebar, inventory product names |

---

## Auto-Fixed (23 issues)

### launch-ready: fix security
- Guard `STRIPE_WEBHOOK_SECRET` with null check (was `!` non-null assertion)
- Close cron endpoint when `CRON_SECRET` unset (was open)
- Close Sanity webhook when `SANITY_WEBHOOK_SECRET` unset (was open)
- Whitelist fields in address PATCH (was mass-assignment vulnerable)
- HTML-escape contact form email body (was XSS injectable)
- Add Zod validation to register route (was no email format check)
- Add CSP and HSTS security headers to next.config.ts
- Add try/catch to `sendEmail` function (was throwing unhandled)

### launch-ready: fix stripe
- Add idempotency guard in webhook (check existing order by stripeSessionId)
- Fix inventory rollback using wrong quantity (was using current item's qty for all prior reservations)
- Wrap webhook sendEmail in try/catch (email failure no longer crashes webhook)

### launch-ready: fix orthography
- `Pagina` -> `Pagina` in es.json Common.pageNotFound
- `pagina` -> `pagina` in es.json Common.pageNotFoundText
- `recuperacion` -> `recuperacion`, `valido` -> `valido` in Cart.recoverExpired
- `Mensaje Enviado!` -> `Mensaje Enviado!` in Contact.successTitle

### launch-ready: fix seo
- Fix canonical URLs in 3 email templates (blossombyolha.com -> www.blossombyolha.com)
- Fix cart recovery URL fallback to use www prefix

### launch-ready: fix accessibility
- Add skip-to-content link in locale layout
- Add `id="main-content"` to main element

### launch-ready: fix ux
- Create `[locale]/error.tsx` error boundary with Sentry integration
- Remove console.log from webhook (replaced with comments or console.error)

---

## P0 — Must Fix Before Deploy (8 remaining)

### Security
1. **No rate limiting on login endpoint** (`/api/auth/[...nextauth]`). Brute-force vulnerable.
   File: `src/middleware.ts` or create rate limit wrapper for NextAuth

### Email
2. **No unsubscribe link in cart recovery emails**. GDPR/CAN-SPAM violation for marketing emails.
   File: `src/emails/cart-recovery.tsx` + `src/emails/components/Layout.tsx`

3. **Welcome email template exists but is never triggered**. New users get no confirmation.
   File: `src/app/api/auth/register/route.ts` — add sendEmail with Welcome template

### Database
4. **Missing DB indexes** on 7 FK columns. Slow queries under load.
   `OrderItem.orderId`, `OrderItem.sanityProductId`, `OrderStatusHistory.orderId`, `StockMovement.inventoryId`, `StockMovement.orderId`, `Address.userId`, `EmailLog.to`

5. **Inventory reservation not transactional**. Concurrent checkouts can oversell.
   File: `src/app/api/checkout/route.ts` — wrap in `db.$transaction`

### Stripe
6. **Order number generation race condition**. Concurrent webhooks can create duplicates.
   File: `src/app/api/webhook/route.ts` — wrap in `db.$transaction` with serialized isolation

### Links/SEO
7. **Missing `/public/og-image.jpg`**. All pages have broken Open Graph image.

### Role Empathy (Launch Blocker)
8. **No password reset flow**. Customers who forget passwords are permanently locked out.

---

## P1 — Fix Soon (12)

### Accessibility
- `<html lang="en">` hardcoded in root layout (HtmlLangUpdater fixes client-side but SSR sends "en")
- Low aria-label coverage on icon-only buttons
- Limited keyboard event handling on custom components
- No `aria-describedby` on form error messages

### Email
- Order confirmation uses raw HTML instead of React Email template
- No newsletter double opt-in confirmation

### Code Quality
- 41 empty catch blocks silently swallow errors
- Duplicated CartItem interface (3 files)
- Duplicated country list (2 files)
- Order date format hardcoded to 'en-GB' ignoring locale

### Dependencies
- next-auth v5 still in beta
- @notionhq/client appears unused

---

## P2 — Recommendations (15)

- Remove duplicate sitemap system (sitemap.ts vs next-sitemap)
- Move AI SDKs behind server-only imports
- Consider Redis-backed rate limiting for multi-instance production
- Add logo image to email layout
- Add FROM_EMAIL env var for flexibility
- Add `@@index([guestEmail])` on Order model
- Translate privacy/terms pages or move outside [locale] routing
- Translate aria-label attributes
- Verify focus ring visibility on interactive elements
- Inspect Resend result.error in callers
- Add address delete confirmation dialog
- Address delete error handling
- Wishlist "product unavailable" placeholder card
- Framer-motion lazy loading
- Homepage images missing sizes prop

---

## Test & Demo Accounts

### Test Accounts (dev only)
| Role | Email | Password |
|------|-------|----------|
| admin | admin@blossomjewellery.art | changeme-in-production |
| store_owner | olha@blossomjewellery.art | changeme-in-production |

**Note**: Admin credentials still use old domain. Passwords must be changed before launch.

---

## Next Steps (Priority Order)

1. Create `/public/og-image.jpg` (1200x630, branded)
2. Implement password reset flow (forgot password link + reset endpoint + email)
3. Add DB migration for missing indexes
4. Wrap inventory reservation in `$transaction`
5. Add unsubscribe mechanism to cart recovery emails
6. Wire up welcome email on registration
7. Add rate limiting to NextAuth login endpoint
8. Fix mobile admin sidebar toggle button
9. Show product names instead of Sanity IDs in inventory page
10. Populate Sanity CMS with real products (launch blocker from previous audit)
